version: "3.8"
services:
  client:
    build: /client
    ports:
      - "3000:3000"
    volumes:
      - ./client:/client
      - /client/node_modules
  server:
    build: /server
    ports:
      - "5000:5000"
    volumes:
      - ./server:/server
      - /server/node_modules
